#include <stdio.h>

int main()
{
    int currentage;
    printf("my current age is :");
    scanf("%d",&currentage);
    currentage++;
    printf("my next year age is :%d",currentage);
    currentage--;
    currentage--;
    printf("\nmy previous year age is :%d",currentage);
}
