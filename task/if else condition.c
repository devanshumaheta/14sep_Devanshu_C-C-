#include <stdio.h>
void main()
{
    int a,b;
    printf("enter the value :");
    scanf("%d%d",&a,&b);
    if(a>b)
    {
        printf("a no is maximum.%d",a);
    
    }
    else
    {
        printf("a no is minimun.%d",a);
        
    }
}
